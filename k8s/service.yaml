apiVersion: v1
kind: Service
metadata:
  name: retailpulse-service
  labels:
    app: retailpulse
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: retailpulse
